# .env.example
# Template file - copy to .env and fill with real values
# DO NOT commit real .env file

DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
OLTP_DB=olist_oltp
OLAP_DB=olist_olap
SQLITE_PATH=data/olist.sqlite